name: Package and Release Scripts

on:
  push:
    branches:
      - main  # Change to master or your primary branch if different
    paths:
      - 'Scripts/**'

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create Zip Archive
        run: |
          mkdir ParryIndicator
          cp -r Scripts/* ParryIndicator/
          cp enabled.txt ParryIndicator/
          zip -r ParryIndicatorMk2.zip ParryIndicator

      - name: Set release version
        id: release_version
        run: echo "short_sha=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Create Release and Upload Asset
        uses: softprops/action-gh-release@v2
        with:
          tag_name: release-${{ steps.release_version.outputs.short_sha }}
          name: Release ${{ steps.release_version.outputs.short_sha }}
          draft: false
          prerelease: false
          files: ParryIndicatorMk2.zip
